const {
  consultaSQL,
  insertUser,
  deleteUser,
  updateUser,
  queryUser,
} = require("./queries");

// Llamamos a la función para ejecutar la consulta
//insertUser("Percy", "12.345.678-9", "Batería", "1");
//deleteUser("Percy");
//consultaSQL();
//queryUser("Percy");
//updateUser("Percy", "percy");
